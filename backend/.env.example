# ── Server ──────────────────────────────────
PORT=5000
NODE_ENV=development

# ── JWT ──────────────────────────────────────
JWT_SECRET=parksmart_super_secret_key_change_in_production
JWT_EXPIRES_IN=7d

# ── MongoDB (primary database) ───────────────
MONGO_URI=mongodb+srv://parkingsystem:parkingsystem123@parkingsystem.mbdbguc.mongodb.net/

# ── MySQL (optional – for relational reports) ─
MYSQL_HOST=localhost
MYSQL_PORT=3306
MYSQL_USER=root
MYSQL_PASSWORD=yourpassword
MYSQL_DATABASE=parksmart

# ── Frontend origin (for CORS) ────────────────
CLIENT_ORIGIN=http://localhost:3000
